# QWB 2019 SuperSqli

version: "2"

services:

  web:
    build: .
    image: ctftraining/qwb_2019_supersqli
    restart: always
    ports:
      - "127.0.0.1:8302:80"
    environment:
      - FLAG=flag{glzjin_wants_a_girl_firend}
